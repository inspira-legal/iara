<repos>
Your working directory is NOT a git repository. All code and git operations must happen inside the repo directories listed below.

{{repo_list}}

IMPORTANT: Always `cd` into a repo directory before running git commands or editing files. Use relative paths like `cd ./{{example_repo_name}}` from the working directory.
</repos>
