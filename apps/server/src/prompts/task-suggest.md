The user wants to work on: "{{userGoal}}"

Analyze the branch naming pattern of EACH repo and create names that follow
the existing pattern. Branches may have DIFFERENT names across repos.
If the repo has no clear pattern, use conventional branches: feat/, fix/, chore/, refactor/, docs/.
Suggest a short, descriptive name for the task.
