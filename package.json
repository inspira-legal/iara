{
  "name": "@iara/monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "scripts"
    ],
    "catalog": {
      "@types/node": "^24.10.13",
      "tsdown": "^0.20.3",
      "typescript": "^5.7.3",
      "vitest": "^4.0.0"
    }
  },
  "type": "module",
  "scripts": {
    "dev:desktop": "bun scripts/dev-runner.ts",
    "build": "turbo run build",
    "build:desktop": "turbo run build --filter=@iara/desktop --filter=@iara/web --filter=@iara/server",
    "build:server": "turbo run build --filter=@iara/server",
    "dev:server": "cd apps/server && bun run dev",
    "typecheck": "turbo run typecheck",
    "lint": "oxlint --report-unused-disable-directives",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "electron-smoke": "bun scripts/electron-smoke.ts",
    "release-smoke": "bun scripts/release-smoke.ts",
    "release:linux": "bun scripts/release/index.ts --platform linux",
    "release:mac": "bun scripts/release/index.ts --platform mac",
    "release:win": "bun scripts/release/index.ts --platform win --wsl-server",
    "release:wsl-server": "bun scripts/release/prepare-wsl-server.ts",
    "package:server": "bun build:server",
    "knip": "knip",
    "prepare": "lefthook install",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/*/dist apps/*/dist-electron packages/*/dist .turbo apps/*/.turbo packages/*/.turbo"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "@xterm/addon-webgl": "^0.19.0",
    "class-variance-authority": "^0.7.1",
    "tree-kill": "^1.2.2"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "^4.1.0",
    "knip": "^6.0.1",
    "lefthook": "^2.1.4",
    "oxfmt": "^0.40.0",
    "oxlint": "^1.55.0",
    "turbo": "^2.8.19",
    "vitest": "catalog:"
  },
  "overrides": {
    "vite": "^8.0.0"
  },
  "engines": {
    "bun": "^1.3.9",
    "node": "^24.13.1"
  },
  "packageManager": "bun@1.3.9"
}
